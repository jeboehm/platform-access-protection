---

name: Publish extension
on:
  workflow_dispatch:

jobs:
  build_and_publish:
    uses: shopware/github-actions/.github/workflows/store-release.yml@a155002e7da62ef2572697fe7d2fe981b7acef78
    with:
      extensionName: JeboehmAccessProtection
    secrets:
      accountUser: ${{ secrets.ACCOUNT_USER }}
      accountPassword: ${{ secrets.ACCOUNT_PASSWORD }}
      ghToken: ${{ secrets.GITHUB_TOKEN }}
